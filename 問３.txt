
intの割り算→切り捨て

++n 実行前に反映
n++ 実行後に反映

配列の形
int x[] = new x[]

8進数に数字の8は出てこない（０から７の８個の数字という意味なため）

異なる変数同士の計算結果は、大きい方の型にして受け取る

Stringは代入してるから基本データ型に見えて、参照型

実数値はビット列で表現してるため、誤差が出るときがある

byteは127〜-128、それを超えると逆の方に戻ってしまうため
byte x = 127
x += 1
すると、127を超えたため、逆の-128になる